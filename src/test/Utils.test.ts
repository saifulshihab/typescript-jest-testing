import { Utils } from '../app/Utils';

describe('Utils file test', () => {
  test('test workfs', () => {
    const upperStr = Utils.toUpperCase('shihab');
    console.log('first test works successfully!');
  });
});
